# Machine Audit — 2026-07-06 (old laptop, macOS 26.5.1)

Snapshot used to build the migration plan. Homebrew 6.0.5.

## Brew formulae (leaves)

awscli, bcrypt, direnv, docker, fzf, gh, git, git-filter-repo, jq, libpq,
node, nvm, pnpm, poppler, postgresql@18, python@3.12, render, ripgrep,
speedtest-cli, supabase, tree, uv, yt-dlp

## Brew casks

1password, 1password-cli, anylist, copyclip, docker-desktop, elgato-camera-hub,
elgato-control-center, elgato-stream-deck, iina, iterm2, keyboard-maestro,
notion, obs, obsidian, orbstack, postman, raycast, slack, tableplus,
visual-studio-code

## Hand-installed apps (moved to Brewfile casks or mas)

Claude, CleanMyMac 5, Commander One PRO, Google Chrome, Linear, Plottr,
Remote Desktop, Scrivener, Telegram, TurboTax 2025, Under My Roof, WhatsApp,
Keynote/Pages/Numbers/GarageBand/iMovie (App Store)

## Dev environment

- Node via nvm: v24.14.0, v24.15.0, v25.8.1, v25.8.2, v25.9.0 (plan: LTS only)
- npm globals: corepack, fast-cli, mint, npm
- Python 3.14.3 (brew python3) + python@3.12 formula; uv installed, no uv tools
- Git: signing via 1Password SSH (op-ssh-sign), gh credential helper, pull.rebase
- VS Code: 100+ extensions → vscode-extensions.txt (heavy Salesforce block = Phase2)

## Shell

- zsh + oh-my-zsh (robbyrussell base, custom prompt), plugins: git
- ~/.zshrc, ~/.zshenv (nvm lazy-load), ~/.zprofile (brew shellenv, OrbStack, Obsidian CLI)
- ~/.zsh.d/aliases.mine.zsh — project nav system (cb/lwd/mhc/hcs/pdm/ref + tab completion)
- direnv hooked (1 allowed dir), iTerm2 shell integration
- Login items: Keyboard Maestro Engine, Raycast, Evernote (dead — app removed)

## Secrets locations (inventory only — values in 1Password)

- ~/.ssh: config only (1Password IdentityAgent — no key files on disk)
- ~/.aws: config, credentials, aws-api-mcp
- ~/.config: op, gh, git/allowed_signers, pm2, raycast, cagent
- ~/.render, ~/.supabase, ~/.sfdx, ~/.docker
- .env files: personal-dam, pims-ops-portal (.env.local), media-vault-ops,
  mhc-control-panel-legacy (.env + .envrc), mhc-cp

## Services (see services-and-ports.md for the living version)

- brew services: postgresql@18 (stopped), unbound (dependency, stopped)
- Docker: OrbStack context active; Docker Desktop also installed (redundant)
- pm2: mhc-dev, personal-dam-dev via ~/.config/pm2/ecosystem.config.cjs
- launchd (custom): com.refineo.apple-card-filer (watches ~/Downloads/Personal)
- Old postgresql@16 data dir still present (not migrated)

## Repos (19) — see scripts/05-repos.sh for slugs

Orgs: tracycsmith, Refineo-Studios-LLC, Ryder-Industries-LLC, TinyCB, phase2, fpgaminer (fork)

## Oddities found

- Stray jpg file in /Applications (junk)
- Evernote login item with no app
- mas not installed (added to new Brewfile)
- Both docker-desktop and orbstack installed; context = orbstack
